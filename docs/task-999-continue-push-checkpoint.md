# Task 999 — Continue push checkpoint while Q0 mapping runs

## 3 lát cắt nhỏ nên mở tiếp ngay
1. **Public story detail → related navigation cue**: thêm một microchange public nhỏ giúp story detail dẫn người dùng rõ hơn sang guides/tickets, vì đây là chỗ public đã có tín hiệu slice tốt nhất.
2. **Secondary-admin exact file-path microchange**: đẩy tiếp 1 task rất hẹp ở secondary-admin nhưng bắt buộc output exact file path để checkpoint sau verify chắc hơn.
3. **Shared admin summary/placeholder consistency cue**: thêm 1 microchange nhỏ ở shared admin layer để đồng bộ wording/cue giữa các placeholder và summary blocks vừa được đụng gần đây.
