# Task 1226 — Web-demo completion checkpoint after T1-T5

1. **Public vừa chốt thêm**: public/support cues và shared/public wrapper quality đã tiến thêm qua các batch verified gần đây, đặc biệt quanh `top-picks`, `story/detail`-style guidance và shared wrapper cleanup.
2. **Còn 1–3 vùng đáng đẩy tiếp nếu chưa xong**: (a) một pass parity cuối giữa `guides` / `top-picks` / `story detail`, (b) một next-step/CTA cue rõ hơn ở public story/detail hoặc guides, (c) một lượt kiểm tra demo-run để bắt đúng chỗ còn cấn thay vì đoán tiếp.
3. **Verdict**: **đã gần xong**, nhưng vẫn nên có **thêm một push rất hẹp hoặc một demo-run pass** trước khi gọi là showable hoàn toàn.
