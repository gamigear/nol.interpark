# Task 998 — Next push checkpoint from recent completed slices

## 3 lát cắt nhỏ nên mở tiếp ngay
1. **Public story detail support block follow-up** — sau `stories/[slug]` và `guides`, mở 1 micro-slice rất hẹp để làm rõ hơn mối nối story ↔ guides/tickets bằng 1 block support/CTA tĩnh nhỏ ngay trong route public.
2. **Secondary-admin operator cue with exact file-path reporting** — tiếp tục 1 microchange cho 1 file secondary-admin chưa có note/hint rõ, nhưng lần này bắt buộc trả exact file path trong result để checkpoint verify chắc hơn.
3. **Shared admin placeholder/summary consistency cue** — thêm 1 microchange nhỏ ở shared admin support layer để các placeholder/summary blocks nhìn đồng đều hơn với các operator cues vừa hoàn tất ở admin/public.
