# Task 1021 — Public-wave checkpoint for next parallel push

1. **Top-picks → stronger next-step/support cue**: thêm một microchange public nhỏ ở `top-picks` để CTA/support cue rõ hơn, vì đây là route public đang ít được chạm hơn story/tickets nhưng nhìn thấy ngay.
2. **Guides/story consistency pass**: chọn 1 trong `guides` hoặc `stories/[slug]` để thêm hoặc tinh chỉnh một support/related-content cue nhỏ, giúp public slice nhìn đồng đều hơn thay vì mỗi route một mức dẫn hướng khác nhau.
