# Task 1066 — Recheck batch E: concise public checkpoint

1. **Story detail → stronger onward CTA block**: thêm một microchange rất nhỏ trong `stories/[slug]` để người đọc có next-step rõ hơn sang guides/tickets/related reading, vì đây là public area leverage cao và nhìn thấy ngay.
2. **Guides/Top-picks parity refinement**: chọn route public còn lại ít đồng đều hơn (`guides` hoặc `top-picks`) và thêm 1 support/empty-state cue nhỏ để public-facing pages nhìn nhất quán hơn sau khi stale tasks đã được dọn.
