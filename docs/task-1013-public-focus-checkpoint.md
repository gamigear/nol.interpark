# Task 1013 — Public-focus checkpoint for next parallel push

1. **Story detail → stronger related-content / next-step cue**: thêm một microchange rất nhỏ trong `stories/[slug]` để người đọc có hành động kế tiếp rõ hơn sang guides/tickets/related reading.
2. **Guides/Top-picks parity pass**: chọn 1 route public còn lại (`guides` hoặc `top-picks`) và thêm 1 support cue/empty-state refinement nhỏ để các route public nhìn đồng đều hơn thay vì mỗi route một mức polish khác nhau.
