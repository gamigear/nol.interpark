# Remount Request — world.nol App Workspace

**To:** User/Admin  
**Re:** Workspace remount for world.nol.com rebuild project

---

## Request

Please remount the **world.nol.com app repository** (source code) to the current workspace. Current workspace is missing the app source tree, blocking public code tasks.

**What we need:**
- Full app repo (Next.js + CMS code) mounted to workspace root or `/app`
- Verify: `package.json`, `src/`, `public/` directories visible

**Why:** Multiple public demo tasks are blocked waiting for source access to verify/fix code.

---

**Once remounted, we can resume immediately.**
